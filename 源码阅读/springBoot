Spring Application 初始化配置：

SpringApplicationBuilder: springApplication 的机构方式
	提供Source Banner isWebEnv 的环境配置方式。
	提供不同的ApplicationContextInit 方式。

org.springframework.core.io.support.SpringFactoriesLoader#loadFactoryNames
	加载对应的 META-INF/spring.factories 目录下的配置信息。
	对去对应的 获取不同的Jar 包中的 factories 启动配置信息。


Spring Application 初始化操作：
	1 web Context 的判断， "javax.servlet.Servlet", "org.springframework.web.context.ConfigurableWebApplicationContext" 同时存在的时候，才是Web 环境。
	2 获取 ApplicationContextInitializer 在 ApplicationContext 初始化后 回调接口。
		通过 不同的Jar 读取 spring.factories 内容。
		获取 Properties 中配置信息，  （spring.properties）, 初始化。
		获取初始化信息。
	3 获取 ApplicationListener 配置接口， 不同的Jar 中的 spring.properties 数据。
	4 发现MainClass 的配置信息
		deduceMainApplicationClass 都是通过 Main 执行到现在， 
		通过 StackTrace() 树 查找对应的Main Class 。

Spring Application， 初始化 resource,  banner 等配置信息。
child 启动配置方式， 

SpringApplication run 的启动方式
	1 spring 默认 ApacheCommon 的 日志加载。 记录启动的时间日志配置。

